apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: consul
  namespace: vault
  annotations:
    volume.beta.kubernetes.io/storage-class: "aws-efs"
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Gi
